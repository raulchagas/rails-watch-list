<div class="container">
  <h1>Create a New Movie List</h1>
  <%= simple_form_for(@list) do |f|  %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>
  <hr>
  <%= link_to "Back to Lists", lists_path %>
</div>
